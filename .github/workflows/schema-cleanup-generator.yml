name: Generate consolidated schema cleanup

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporarily disabled while the cleanup payload is updated.'
