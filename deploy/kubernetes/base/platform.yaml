apiVersion: v1
kind: Namespace
metadata:
  name: unrealdb
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: unrealdb-config
  namespace: unrealdb
data:
  UNREALDB_SITE_NAME: Unreal File Catalog
  UNREALDB_STORAGE_PATH: /var/www/html/catalog/storage
  UNREALDB_MAX_UPLOAD_BYTES: "268435456"
  UNREALDB_REMEMBER_DAYS: "30"
  UNREALDB_SESSION_COOKIE_SECURE: "1"
  UNREALDB_QUEUE_NAME: catalog
  UNREALDB_QUEUE_LEASE_SECONDS: "120"
  UNREALDB_WORKER_MAX_JOBS: "100"
  UNREALDB_WORKER_SLEEP_MS: "250"
  UNREALDB_WORKER_IDLE_SECONDS: "2"
  UNREALDB_WORKER_ERROR_SECONDS: "10"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unrealdb-storage
  namespace: unrealdb
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: unrealdb-web
  namespace: unrealdb
  labels:
    app.kubernetes.io/name: unrealdb
    app.kubernetes.io/component: web
spec:
  replicas: 1
  revisionHistoryLimit: 5
  progressDeadlineSeconds: 600
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: unrealdb
      app.kubernetes.io/component: web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: unrealdb
        app.kubernetes.io/component: web
    spec:
      terminationGracePeriodSeconds: 90
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: web
          image: ghcr.io/ardenee/ut_reader:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: unrealdb-config
            - secretRef:
                name: unrealdb-secrets
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
          startupProbe:
            httpGet:
              path: /catalog/api/v1/live.php
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /catalog/api/v1/health.php
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /catalog/api/v1/live.php
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 10"]
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
              add: ["CHOWN", "DAC_OVERRIDE", "SETGID", "SETUID"]
          volumeMounts:
            - name: storage
              mountPath: /var/www/html/catalog/storage
      volumes:
        - name: storage
          persistentVolumeClaim:
            claimName: unrealdb-storage
---
apiVersion: v1
kind: Service
metadata:
  name: unrealdb-web
  namespace: unrealdb
  labels:
    app.kubernetes.io/name: unrealdb
    app.kubernetes.io/component: web
spec:
  selector:
    app.kubernetes.io/name: unrealdb
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: unrealdb-worker
  namespace: unrealdb
  labels:
    app.kubernetes.io/name: unrealdb
    app.kubernetes.io/component: worker
spec:
  replicas: 1
  revisionHistoryLimit: 5
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: unrealdb
      app.kubernetes.io/component: worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: unrealdb
        app.kubernetes.io/component: worker
    spec:
      terminationGracePeriodSeconds: 90
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: worker
          image: ghcr.io/ardenee/ut_reader:latest
          imagePullPolicy: IfNotPresent
          args:
            - /var/www/html/deploy/docker/worker-loop.sh
          envFrom:
            - configMapRef:
                name: unrealdb-config
            - secretRef:
                name: unrealdb-secrets
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
              add: ["CHOWN", "DAC_OVERRIDE", "SETGID", "SETUID"]
          volumeMounts:
            - name: storage
              mountPath: /var/www/html/catalog/storage
      volumes:
        - name: storage
          persistentVolumeClaim:
            claimName: unrealdb-storage
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: unrealdb
  namespace: unrealdb
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: 256m
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "30"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - unrealdb.example.com
      secretName: unrealdb-tls
  rules:
    - host: unrealdb.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: unrealdb-web
                port:
                  name: http
